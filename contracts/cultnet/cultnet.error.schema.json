{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/GameCult/CultLib/contracts/cultnet/cultnet.error.schema.json",
  "title": "CultNet Error Message",
  "type": "object",
  "required": [
    "schemaVersion",
    "error"
  ],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": { "const": "cultnet.error.v0" },
    "error": { "type": "string", "minLength": 1 },
    "code": {
      "description": "Machine-readable refusal code (R-N). Additive: a v0 peer that does not know this field ignores it.",
      "type": "string",
      "enum": ["selection_invalid", "cursor_stale", "cursor_invalid", "reference_outside_target"]
    },
    "details": {
      "description": "Code-specific structured detail (R-N). Every field is populated only for the code that defines it.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "field": { "description": "selection_invalid: the selection field that failed.", "type": "string" },
        "value": { "description": "selection_invalid: the offending value, when there is one string worth naming.", "type": "string" },
        "asOf": { "description": "cursor_stale: the cursor's own minted asOf.", "type": "integer", "minimum": 0 },
        "current": { "description": "cursor_stale: the answering server's current asOf.", "type": "integer", "minimum": 0 }
      }
    }
  }
}
